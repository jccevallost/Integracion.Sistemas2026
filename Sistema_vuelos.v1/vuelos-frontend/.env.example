# ============================================================
#   VUELOS FRONTEND — .env
# ============================================================
# Copia como .env y ajusta según el entorno.

# En Reto 1: apunta al monolito
VITE_API_URL=http://localhost:3800/api/v1

# En Reto 2: apunta al API Gateway (mismo contrato, diferente host)
# VITE_API_URL=http://localhost:8080/api/v1
